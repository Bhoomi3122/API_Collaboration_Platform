/**
 * @deprecated WorkspaceSidebar is no longer used.
 * The app uses GlobalNavDrawer (components/common/GlobalNavDrawer.jsx) as the single sidebar everywhere.
 */
export default function WorkspaceSidebar() {
  return null;
}
